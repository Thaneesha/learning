version: 0.2
env:
  variables:
    build_version: "0.1"
phases:
  install:
    commands:
      - apt-get install git
  build:
    commands:
      - git checkout master
      - git merge dev
      - git push origin master 
